# 06-16 中考英语语法填空 练习二

主题：乐观心态 (An Optimistic Attitude)

## 一、题目

阅读下面短文，在空白处填入一个适当的单词，或填入括号中所给单词的正确形式。

Life is full of ups and downs, but (1) ______ optimistic attitude can help us face challenges (2) ______ (brave). When I (3) ______ (fail) my first English test in Grade Eight, I felt upset and thought it was impossible for me (4) ______ catch up. My teacher advised me to write down three small goals, and I tried (5) ______ (follow) her advice. Two months later, my grades improved (6) ______ (slow) than before. Now I know that optimism comes (7) ______ small habits, not luck. If we keep trying, we will find it (8) ______ (easy) to solve problems. "Every cloud has (9) ______ silver lining," my deskmate often says. So let's smile and treat each day (10) ______ a new beginning.

---

## 二、答案与解析

1. **答案：an**  
   - **考点**：冠词。`optimistic` 以元音音素开头，表示“一种乐观的态度”用 `an optimistic attitude`。与 `a positive attitude` 对比记忆。

2. **答案：bravely**  
   - **考点**：词性转换。修饰动词 `face`，需用副词；`brave` 变为 `bravely`（以 `e` 结尾去 `e` 加 `ly`）。

3. **答案：failed**  
   - **考点**：动词时态。回忆八年级第一次考试失利，为过去事件，用一般过去时 `failed`。

4. **答案：to**  
   - **考点**：非谓语动词/固定句型。`It is + adj. + for sb. + to do sth.` 中，真正主语是不定式，需用 `to catch up`。看到 `for me` 后常接 `to do`。

5. **答案：to follow**  
   - **考点**：非谓语动词。`try to do sth.` 表示“努力去做某事”，符合语境“努力听从建议”；`try doing` 多表示“试着做看看”，此处用 `to follow` 更贴切。

6. **答案：more slowly**  
   - **考点**：副词比较级。句中有 `than`，且 `slow` 修饰动词 `improved`（描述进步的过程），用副词 `slowly` 的比较级 `more slowly`（多音节副词前加 `more`）。

7. **答案：from**  
   - **考点**：介词。`come from` 表示“来源于”，说明乐观来自小习惯。也可联想 `result from`。

8. **答案：easier**  
   - **考点**：形容词比较级。`find it + adj. + to do` 结构中，`it` 为形式宾语；语境暗含与过去对比，用 `easy` 的比较级 `easier`。

9. **答案：a**  
   - **考点**：冠词与习语。谚语 `Every cloud has a silver lining`（黑暗中总有一线光明）为固定表达，`silver` 以辅音音素开头，用 `a`。

10. **答案：as**  
    - **考点**：介词。`treat...as...` 为固定搭配，意为“把……当作……”。根据动词 `treat` 的搭配选择 `as`。
